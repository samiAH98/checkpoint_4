{% extends 'base.html.twig' %}

{% block title %}Menbres{% endblock %}

{% block body %}
    <h1>Menmbres</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Email</th>
                <td>{{ user.email }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ user.lastname }}</td>
            </tr>
            <tr>
                <th>Prénom</th>
                <td>{{ user.firstname }}</td>
            </tr>
        </tbody>
    </table>

{#    <a href="{{ path('app_user_index') }}">back to list</a>#}

    <a href="{{ path('app_user_edit', {'id': user.id}) }}">Modifier</a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
