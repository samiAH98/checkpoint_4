<form method="post" action="{{ path('app_cake_delete', {'id': cake.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ cake.id) }}">
    <button class="btn">Delete</button>
</form>
